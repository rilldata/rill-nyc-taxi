# Visit https://docs.rilldata.com/reference/project-files/sources to learn more about Rill source files.

type: "duckdb"
sql: SELECT * FROM read_csv('s3://nyc-tlc/misc/taxi _zone_lookup.csv', auto_detect=true, ignore_errors=1, header=true)